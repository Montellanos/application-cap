import { PermissFormatPipe } from './permiss-format.pipe';

describe('PermissFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PermissFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
